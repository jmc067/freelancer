web:
    build: .
    command: python -u app.py
    ports:
        - "5000:5000"
    volumes:
        - .:/app
    env_file:
        - variables.env   
    links:
        - db
db:
    image: mongo:3.0.2
redis:
    image: redis

